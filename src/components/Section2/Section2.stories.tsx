import { Meta, StoryFn } from "@storybook/react";
import { Section2 } from "./Section2";

export default {
    title: "Section2",
    component: Section2,
} as Meta;

export const Default: StoryFn = () => <Section2 />;
